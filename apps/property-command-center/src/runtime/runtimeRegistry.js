import{createCapabilityManifest,validateCapabilityManifest}from'./capabilityManifest.js';
const registry=new Map(),history=[];const record=(type,payload)=>history.push(Object.freeze({sequence:history.length+1,type,payload,recordedAt:new Date().toISOString()}));
export function registerCapability(input={}){const manifest=createCapabilityManifest(input),validation=validateCapabilityManifest(manifest);if(!validation.valid)throw new Error(validation.errors.join(','));const current=registry.get(manifest.id);if(current&&current.version===manifest.version)return current;registry.set(manifest.id,manifest);record(current?'CAPABILITY_UPDATED':'CAPABILITY_REGISTERED',{id:manifest.id,version:manifest.version});return manifest}
export function unregisterCapability(id,{allow=false}={}){if(!allow)throw new Error('Governed unregister approval required');const existed=registry.delete(String(id));if(existed)record('CAPABILITY_UNREGISTERED',{id:String(id)});return existed}
export function getCapability(id){return registry.get(String(id))||null}export function listCapabilities(){return[...registry.values()].sort((a,b)=>a.id.localeCompare(b.id))}export function readRuntimeHistory(){return[...history]}export function resetRuntimeRegistry(){registry.clear();history.length=0}
