const flags=new Map(),audit=[];const normalize=v=>Boolean(v);
export function defineFeatureFlag(name,options={}){const key=String(name);if(!key)throw new Error('Feature flag name required');if(!flags.has(key))flags.set(key,{name:key,enabled:normalize(options.enabled),scope:options.scope||'GLOBAL',description:options.description||''});return flags.get(key)}
export function setFeatureFlag(name,enabled,context={}){const flag=defineFeatureFlag(name);const next={...flag,enabled:normalize(enabled),updatedBy:context.actorId||'SYSTEM',updatedAt:new Date().toISOString()};flags.set(String(name),next);audit.push({sequence:audit.length+1,name:String(name),enabled:next.enabled,actorId:next.updatedBy,reason:context.reason||'UNSPECIFIED'});return next}
export function isFeatureEnabled(name,{overrides={}}={}){if(Object.prototype.hasOwnProperty.call(overrides,name))return Boolean(overrides[name]);return Boolean(flags.get(String(name))?.enabled)}export function listFeatureFlags(){return[...flags.values()]}export function readFeatureFlagAudit(){return[...audit]}export function resetFeatureFlags(){flags.clear();audit.length=0}
