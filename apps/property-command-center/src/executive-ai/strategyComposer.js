export function composeStrategy({objective,options=[],constraints=[],horizon='90_DAYS'}={}){if(!objective)throw new Error('objective required');const ranked=[...options].sort((a,b)=>(b.score||0)-(a.score||0));return Object.freeze({objective,horizon,constraints:Object.freeze([...constraints]),options:Object.freeze(ranked.map(Object.freeze)),recommendedOption:ranked[0]||null,approvalRequired:true})}
