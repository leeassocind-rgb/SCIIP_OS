export function explainDecision({inputs=[],weights={},score=0,evidenceIds=[],limitations=[]}={}){const contributions=inputs.map(i=>{const name=i.name||i.key;const value=Number(i.value)||0;const weight=Number(weights[name]??i.weight??0);return{name,value,weight,contribution:Number((value*weight).toFixed(4))}}).sort((a,b)=>Math.abs(b.contribution)-Math.abs(a.contribution));return{score:Number(score),contributions,evidenceIds:[...evidenceIds],limitations:[...limitations],explainable:true}}
