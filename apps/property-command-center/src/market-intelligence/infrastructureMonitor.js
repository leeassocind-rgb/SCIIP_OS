export function normalizeInfrastructureSignal(s={}){return{id:String(s.id||s.name||'SIGNAL'),category:String(s.category||'OTHER').toUpperCase(),impact:String(s.impact||'NEUTRAL').toUpperCase(),description:String(s.description||''),effectiveDate:s.effectiveDate||null,source:s.source||'GOVERNED_INPUT'}}export function prioritizeInfrastructureSignals(signals=[]){const w={HIGH:3,MEDIUM:2,LOW:1,NEUTRAL:0};return signals.map(normalizeInfrastructureSignal).map(s=>({...s,priority:w[s.impact]??0})).sort((a,b)=>b.priority-a.priority)}
