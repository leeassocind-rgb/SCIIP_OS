export function generateMarketBrief({events=[],comparables=[],pipeline={},trends={},signals=[]}={}){const priorities=signals.filter(s=>s.priority>1).length;const headline=`${events.length} market events, ${comparables.length} comparables, and ${priorities} priority infrastructure signals.`;return{headline,sections:{events:events.slice(0,5),comparables:comparables.slice(0,5),pipeline,trends,signals:signals.slice(0,5)},brokerReviewRequired:priorities>0||Boolean(trends.marketTightening),generatedAt:new Date().toISOString(),evidenceRequired:true}}
