export class LiveEventBus{constructor(){this.events=[];this.ids=new Set();this.subscribers=new Map()}publish(e){if(this.ids.has(e.id))return{status:'DUPLICATE',event:e};this.ids.add(e.id);this.events.push(Object.freeze({...e,sequence:this.events.length+1}));for(const fn of this.subscribers.get(e.type)||[])fn(e);return{status:'ACCEPTED',sequence:this.events.length}}subscribe(type,fn){const a=this.subscribers.get(type)||[];a.push(fn);this.subscribers.set(type,a);return()=>this.subscribers.set(type,a.filter(x=>x!==fn))}replay(from=0){return this.events.filter(e=>e.sequence>from)}}
