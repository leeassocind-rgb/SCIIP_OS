export function correlateEvents(events=[],windowMs=86400000){const sorted=[...events].sort((a,b)=>Date.parse(a.occurredAt)-Date.parse(b.occurredAt));const groups=[];for(const e of sorted){let g=groups.find(x=>x.key===e.subjectId&&Date.parse(e.occurredAt)-x.last<=windowMs);if(!g){g={key:e.subjectId,events:[],first:Date.parse(e.occurredAt),last:Date.parse(e.occurredAt)};groups.push(g)}g.events.push(e);g.last=Date.parse(e.occurredAt)}return groups.map(g=>Object.freeze({...g,confidence:g.events.reduce((s,e)=>s+(e.confidence??1),0)/g.events.length}))}
