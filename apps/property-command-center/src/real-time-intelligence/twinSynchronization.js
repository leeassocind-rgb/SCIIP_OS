export class TwinSynchronizer{constructor(){this.state=new Map();this.ledger=[]}apply(event){if(!event.twinId)throw new Error('twinId required');const prev=this.state.get(event.twinId)||{};const next=Object.freeze({...prev,...event.patch,updatedAt:event.occurredAt,eventId:event.id});this.state.set(event.twinId,next);this.ledger.push(Object.freeze({twinId:event.twinId,eventId:event.id,sequence:this.ledger.length+1}));return next}get(id){return this.state.get(id)||null}freshness(id,now=Date.now()){const s=this.get(id);return s?Math.max(0,now-Date.parse(s.updatedAt)):null}}
