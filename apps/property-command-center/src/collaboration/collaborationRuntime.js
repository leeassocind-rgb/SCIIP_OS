import{listComments}from'./commentService.js';import{listNotifications}from'./notificationService.js';import{reviewQueue}from'./reviewQueueService.js';import{readCollaborationLedger}from'./collaborationLedger.js';
export function collaborationSnapshot({assignmentId,userId='current-user'}={}){const comments=listComments({assignmentId}),notifications=listNotifications({assignmentId,userId}),reviews=reviewQueue({assignmentId,reviewerId:userId}),activity=readCollaborationLedger({assignmentId});return Object.freeze({status:'COLLABORATION_OPERATIONAL',assignmentId:assignmentId?String(assignmentId):null,comments,notifications,reviews,activity,metrics:{comments:comments.length,mentions:comments.reduce((n,c)=>n+c.payload.mentions.length,0),unreadNotifications:notifications.filter(n=>n.payload.status==='UNREAD').length,pendingReviews:reviews.filter(r=>['PENDING','IN_REVIEW'].includes(r.state)).length,activityEvents:activity.length}})}
