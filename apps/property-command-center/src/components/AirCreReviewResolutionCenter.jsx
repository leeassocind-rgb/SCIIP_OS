import React,{useMemo,useState}from'react';import{AlertTriangle,CheckCircle2,Filter,ShieldCheck}from'lucide-react';import{readReviewResolutions,saveReviewResolution}from'../production-validation/reviewResolutionRegistry.js';
export default function AirCreReviewResolutionCenter({extractions=[]}){const[filter,setFilter]=useState('ALL');const[resolutions,setResolutions]=useState(()=>readReviewResolutions());const queue=useMemo(()=>extractions.flatMap(d=>d.reviewQueue||[]),[extractions]);const resolved=new Set(resolutions.map(x=>x.reviewId));const visible=queue.filter(x=>!resolved.has(x.reviewId)&&(filter==='ALL'||x.field===filter));const fields=[...new Set(queue.map(x=>x.field))].sort();const approve=x=>setResolutions(saveReviewResolution({reviewId:x.reviewId,recordId:x.recordId,field:x.field,approvedValue:x.value,decision:'APPROVED',sourceEvidence:x.evidence,brokerApprovalRequired:true}));return <section className="pv-registry"><header><div><h3>AIR CRE review resolution</h3><p>Field-level evidence review with immutable broker decisions. Unresolved values remain blocked.</p></div><div><Filter size={15}/><select value={filter} onChange={e=>setFilter(e.target.value)}><option>ALL</option>{fields.map(f=><option key={f}>{f}</option>)}</select></div></header><div className="pv-extraction-banner"><strong>{visible.length.toLocaleString()} unresolved fields</strong><span>{queue.filter(x=>x.critical&&!resolved.has(x.reviewId)).length} critical · {resolutions.length} decisions retained</span></div><div className="pv-table"><table><thead><tr><th>Risk</th><th>Field</th><th>Value</th><th>Confidence</th><th>Reason</th><th>Evidence</th><th>Decision</th></tr></thead><tbody>{visible.slice(0,250).map(x=><tr key={x.reviewId}><td>{x.critical?<span className="pv-status review_required"><AlertTriangle size={13}/>Critical</span>:<span className="pv-status staged">Review</span>}</td><td><strong>{x.field}</strong></td><td>{String(x.value??'—')}</td><td>{x.confidence}%</td><td>{x.reason}</td><td><small>Page {x.evidence?.page} · Record {x.evidence?.recordNo}</small></td><td><button className="pv-secondary" disabled={x.value==null} onClick={()=>approve(x)}><CheckCircle2 size={14}/>Approve</button></td></tr>)}{!visible.length&&<tr><td colSpan="7"><div className="pv-empty"><ShieldCheck size={22}/><strong>No unresolved fields in this filter</strong></div></td></tr>}</tbody></table></div></section>}
