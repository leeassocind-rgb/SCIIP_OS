import React,{useEffect,useMemo,useState}from'react';
import{Search,Command,Building2,Upload,Map,ShieldCheck,BrainCircuit,Settings}from'lucide-react';
const commands=[['executive','Executive Command Center',Command],['ingestion','Production Ingestion',Upload],['buildings','Building Intelligence',Building2],['market','Market Intelligence',Map],['review','Review Center',ShieldCheck],['copilot','Executive Copilot',BrainCircuit],['admin','Administration',Settings]];
export default function CommandPalette({open,onClose,onNavigate}){const[q,setQ]=useState('');useEffect(()=>{if(!open)return;const h=e=>e.key==='Escape'&&onClose();window.addEventListener('keydown',h);return()=>window.removeEventListener('keydown',h)},[open,onClose]);const items=useMemo(()=>commands.filter(x=>x[1].toLowerCase().includes(q.toLowerCase())),[q]);if(!open)return null;return <div className="palette-backdrop" onMouseDown={onClose}><div className="palette" onMouseDown={e=>e.stopPropagation()}><div className="palette-search"><Search size={17}/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search commands and workspaces…"/><kbd>ESC</kbd></div><div className="palette-list">{items.map(([id,label,Icon])=><button key={id} onClick={()=>{onNavigate(id);onClose()}}><Icon size={17}/><span>{label}</span><kbd>↵</kbd></button>)}</div></div></div>}