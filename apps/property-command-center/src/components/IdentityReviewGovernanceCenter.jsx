import React,{useMemo,useState} from 'react';import data from '../production-validation/identity-review-governance-data.json';
export default function IdentityReviewGovernanceCenter(){const [status,setStatus]=useState('PENDING'),[q,setQ]=useState('');const rows=useMemo(()=>data.reviewQueue.filter(x=>(status==='ALL'||x.decision===status)&&`${x.leftProperty.address} ${x.rightProperty.address} ${x.leftProperty.city}`.toLowerCase().includes(q.toLowerCase())),[status,q]);return <section className="workspace-page"><header><p className="eyebrow">Governed Identity Operations</p><h1>Identity Review & Merge Governance</h1><p>Evidence-backed approve, reject, and defer workflow. No fuzzy identity merge is applied silently.</p></header><div className="metric-grid"><article><strong>{data.summary.totalProposals}</strong><span>Total proposals</span></article><article><strong>{data.summary.pending}</strong><span>Pending</span></article><article><strong>{data.summary.approved}</strong><span>Approved</span></article><article><strong>{data.summary.rejected+data.summary.deferred}</strong><span>Rejected / deferred</span></article></div><div className="toolbar"><select value={status} onChange={e=>setStatus(e.target.value)}><option>PENDING</option><option>APPROVED</option><option>REJECTED</option><option>DEFERRED</option><option>ALL</option></select><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search proposal properties"/></div><div className="data-table"><table><thead><tr><th>Proposal</th><th>Left identity</th><th>Right identity</th><th>Score</th><th>Decision</th></tr></thead><tbody>{rows.map(x=><tr key={x.proposalId}><td>{x.proposalId}</td><td>{x.leftProperty.address}<br/><small>{x.leftProperty.city}</small></td><td>{x.rightProperty.address}<br/><small>{x.rightProperty.city}</small></td><td>{x.score}%</td><td>{x.decision}</td></tr>)}</tbody></table></div><p><small>Decisions are executed through the governed decision-ledger CLI and preserved as immutable records.</small></p></section>}
