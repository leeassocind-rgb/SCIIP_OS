import React,{useState}from'react';import{Layers,Maximize2,LocateFixed,Filter,Play,Pause}from'lucide-react';
export default function MapWorkspace({onSelect}){const[playing,setPlaying]=useState(false);return <div className="map-workspace"><div className="map-toolbar"><button><Layers size={16}/>Layers</button><button><Filter size={16}/>Filters</button><button><LocateFixed size={16}/>Locate</button><button><Maximize2 size={16}/>Full screen</button><button onClick={()=>setPlaying(!playing)}>{playing?<Pause size={16}/>:<Play size={16}/>}Timeline</button></div><div className="map-canvas">{Array.from({length:28}).map((_,i)=><button key={i} className={`map-point ${i%5===0?'hot':''}`} style={{left:`${6+(i*19)%88}%`,top:`${8+(i*31)%80}%`}} onClick={()=>onSelect?.(i)}/>)}</div></div>}