export function traverseGraph(start,edges,maxDepth=3){const seen=new Set([start]),queue=[{id:start,depth:0}],out=[];while(queue.length){const n=queue.shift();if(n.depth>=maxDepth)continue;edges.filter(e=>e.from===n.id||e.to===n.id).forEach(e=>{const id=e.from===n.id?e.to:e.from;if(!seen.has(id)){seen.add(id);const row=Object.freeze({id,depth:n.depth+1,via:e.type});out.push(row);queue.push(row)}})}return Object.freeze(out);}
