export function createRelationshipStore(){const edges=[];const keys=new Set();return Object.freeze({append(e){if(!e?.from||!e?.to||!e?.type)throw new Error('relationship fields required');const k=`${e.from}|${e.type}|${e.to}`;if(keys.has(k))return Object.freeze({status:'DUPLICATE_SAFE',edge:edges.find(x=>x.key===k)});const edge=Object.freeze({...e,key:k,sequence:edges.length+1});edges.push(edge);keys.add(k);return Object.freeze({status:'APPENDED',edge})},all:()=>[...edges]});}
