const tok=s=>new Set(String(s??'').toLowerCase().match(/[a-z0-9]+/g)||[]);export function createSemanticIndex(){const docs=new Map();return Object.freeze({index(d){if(!d?.id)throw new Error('document id required');docs.set(d.id,Object.freeze({...d,tokens:tok(`${d.title||''} ${d.text||''}`)}));return d.id},search(q){const qt=tok(q);return [...docs.values()].map(d=>({id:d.id,title:d.title||'',score:[...qt].filter(t=>d.tokens.has(t)).length/Math.max(1,qt.size)})).filter(x=>x.score>0).sort((a,b)=>b.score-a.score||a.id.localeCompare(b.id))}});}
