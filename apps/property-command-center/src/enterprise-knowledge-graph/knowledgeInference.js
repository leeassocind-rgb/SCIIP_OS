export function inferRelationships(entities=[]){const out=[];for(let i=0;i<entities.length;i++)for(let j=i+1;j<entities.length;j++){const a=entities[i],b=entities[j];if(a.city&&a.city===b.city)out.push(Object.freeze({from:a.id,to:b.id,type:'CO_LOCATED',confidence:.8,inferred:true}));if(a.owner&&a.owner===b.owner)out.push(Object.freeze({from:a.id,to:b.id,type:'COMMON_OWNERSHIP',confidence:.9,inferred:true}));}return Object.freeze(out);}
