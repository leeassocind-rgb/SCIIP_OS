import{clone,deepFreeze,required}from'./immutable.js';
export function createTelemetry(){const metrics=[];const traces=[];return deepFreeze({metric(name,value,tags={}){required(name,'name');const m=deepFreeze({name,value:Number(value),tags:clone(tags)});metrics.push(m);return m;},trace(name,durationMs,status='OK'){required(name,'name');const t=deepFreeze({name,durationMs:Number(durationMs),status});traces.push(t);return t;},summary(){const errors=traces.filter(t=>t.status!=='OK').length;const avg=traces.length?traces.reduce((a,t)=>a+t.durationMs,0)/traces.length:0;return deepFreeze({metricCount:metrics.length,traceCount:traces.length,errorCount:errors,averageDurationMs:Number(avg.toFixed(2))});}});}
