#!/usr/bin/env node
const fs=require('fs'),path=require('path');const root=process.cwd(),dir=path.join(root,'dist/apps-script');const files=fs.readdirSync(dir).filter(f=>f.endsWith('.gs')).sort();let target=files.find(f=>f.includes('other'))||files[0];target=path.join(dir,target);let s=fs.readFileSync(target,'utf8');const fn='function sciipTestV83SuperSheetExtractionAccuracy(){var r=SCIIP_V8_3_SUPERSHEET_EXTRACTION_ACCURACY.certify();Logger.log(JSON.stringify(r));return r;}';if(!s.includes('function sciipTestV83SuperSheetExtractionAccuracy'))fs.appendFileSync(target,'\n'+fn+'\n');console.log(JSON.stringify({framework:'SCIIP_V8_3_SUPERSHEET_COMPILED_PATCH',status:'PASSED',target,wrapperPresent:true}));
