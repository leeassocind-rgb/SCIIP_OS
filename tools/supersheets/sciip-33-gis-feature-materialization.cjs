const c=require('./sciip-production-intelligence-common.cjs');const [,,spatial,out]=process.argv,d=c.read(spatial),sites=c.arr(d.sites).length?d.sites:c.arr(d.spatialSites);const features=sites.map((s,i)=>({type:'Feature',id:c.prop(s)||c.id('SITE',i),geometry:s.longitude!=null&&s.latitude!=null?{type:'Point',coordinates:[c.num(s.longitude),c.num(s.latitude)]}:null,properties:{evidenceId:c.evidence(s),spatialIndex:s.spatialIndex||null}}));const valid=features.filter(x=>x.geometry);const o=c.stamp('SCIIP_GIS_FEATURE_MATERIALIZATION','196.33.0',{sourceSites:sites.length,features:features.length,validGeometries:valid.length,missingGeometries:features.length-valid.length,geoJsonReady:true},{featureCollection:{type:'FeatureCollection',features}});c.write(out,o);console.log(JSON.stringify(o,null,2));
