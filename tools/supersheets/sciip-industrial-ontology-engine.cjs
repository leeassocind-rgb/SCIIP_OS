#!/usr/bin/env node
const C=require('./sciip-enterprise-kg-spatial-common.cjs');
const TYPES=['PROPERTY','BUILDING','LAND_PARCEL','CAMPUS','OWNER','TENANT','LEASE','COMPANY','INDUSTRY','BROKER','MUNICIPALITY','UTILITY','SUBSTATION','RAIL','PORT','AIRPORT','FREEWAY','INTERMODAL','INFRASTRUCTURE_ASSET'];
const REL=['OWNS','LEASES','OCCUPIES','ADJACENT_TO','CONNECTED_TO','SERVED_BY','PART_OF','LOCATED_IN','COMPETES_WITH','COMPARABLE_TO','SUPPLIED_BY','NEAR'];
function run(doc){const src=C.sourceRecords(doc);const entities=[];for(const r of src){const pid=C.val(r,['propertyId','buildingId','id','property.id']);if(pid)entities.push({entityId:'PROPERTY-'+C.slug(pid),entityType:'PROPERTY',label:C.val(r,['address','property.address','name'],pid),sourceKey:String(pid)});const tenant=C.val(r,['tenant','tenantName','company','occupant']);if(tenant)entities.push({entityId:'TENANT-'+C.slug(tenant),entityType:'TENANT',label:String(tenant),sourceKey:String(tenant)});const owner=C.val(r,['owner','ownership','ownerName']);if(owner)entities.push({entityId:'OWNER-'+C.slug(owner),entityType:'OWNER',label:String(owner),sourceKey:String(owner)});}const unique=C.uniq(entities,x=>x.entityId);return{framework:'SCIIP_INDUSTRIAL_ONTOLOGY_ENGINE',version:'196.23.0',status:'PASSED',result:{sourceRecords:src.length,entityTypes:TYPES.length,relationshipTypes:REL.length,canonicalEntities:unique.length,ontologyVersion:'1.0.0'},ontology:{entityTypes:TYPES,relationshipTypes:REL,governance:{versioned:true,appendOnly:true,migrationsRequired:true}},entities:unique};}
if(require.main===module){const[i,o]=process.argv.slice(2);if(!i||!o)throw Error('Usage: node script input.json output.json');const x=run(C.readJson(i));C.writeJson(o,x);console.log(JSON.stringify({framework:x.framework,version:x.version,status:x.status,result:x.result},null,2));}module.exports={run,TYPES,REL};
