#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path');const base='outputs/sprint28-live-operations';const req=['sprint28-live-operations-certification.json','production-commit-plan.json','candidate-generation.json','incremental-replay-report.json','continuous-knowledge-graph.json','continuous-digital-twin.json','rollback-recovery-plan.json','governance-dashboard.json'];let failures=req.filter(f=>!fs.existsSync(path.join(base,f))).map(f=>'MISSING:'+f);if(!failures.length){const r=JSON.parse(fs.readFileSync(path.join(base,req[0])));if(r.status!=='LIVE_OPERATIONS_CERTIFIED')failures.push('NOT_CERTIFIED');if(r.governance.commitExecuted||r.governance.canonicalWrites!==0)failures.push('DEFAULT_COMMIT_GOVERNANCE_FAILED');}const dist='dist/apps-script';if(fs.existsSync(dist)&&!fs.readdirSync(dist).some(f=>fs.readFileSync(path.join(dist,f),'utf8').includes('sciipTestV9Sprint28GovernedLiveOperations')))failures.push('COMPILED_TEST_FUNCTION_MISSING');const o={framework:'SCIIP_V9_SPRINT28_GOVERNED_LIVE_OPERATIONS_VERIFICATION',status:failures.length?'FAILED':'PASSED',checks:req.length+3,failures};console.log(JSON.stringify(o));if(failures.length)process.exit(1);
