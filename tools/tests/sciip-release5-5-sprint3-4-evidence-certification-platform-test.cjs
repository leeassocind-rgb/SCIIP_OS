#!/usr/bin/env node
'use strict';
const assert=require('assert');const{build}=require('../supersheets/sciip-evidence-certification-builder.cjs');
const obs={recordId:'OBS-1',propertyId:'PROPERTY-1',recordNo:7,page:4,address:'100 Test Way',city:'Rialto',zip:'92376',confidence:97,reviewRequired:true,fieldStates:{powerAmps:{state:'REVIEW_REQUIRED',reason:'SOURCE_SIGNAL_WITHOUT_POWERAMPS_VALUE',sourceSignal:'Heavy Power Available'}},evidence:{rawLines:['100 Test Way Rialto CA 92376','Heavy Power Available'],columnEvidence:{clear:['32 ft'],loading:['4 DH / 1 GL']}}};
const report={version:'196.3.0',generatedAt:'2026-07-29T00:00:00Z',summary:{documents:1,reviewRequired:1,criticalReviewRequired:0},documents:[{document:{documentId:'DOC-1',fileName:'sample.pdf',fingerprint:'abc123'},observations:[obs],reviewQueue:[{recordId:'OBS-1',field:'powerAmps',reason:'SOURCE_SIGNAL_WITHOUT_POWERAMPS_VALUE',severity:'OPTIONAL'}]}]};
const r=build(report);const tests=[];function t(name,fn){try{fn();tests.push({name,status:'PASSED'})}catch(e){tests.push({name,status:'FAILED',error:e.message})}}
t('framework',()=>assert.equal(r.framework,'SCIIP_EVIDENCE_CERTIFICATION_PLATFORM'));t('version',()=>assert.equal(r.version,'196.4.0'));t('review package created',()=>assert.equal(r.evidencePackages.length,1));t('source document retained',()=>assert.equal(r.evidencePackages[0].source.fileName,'sample.pdf'));t('source page retained',()=>assert.equal(r.evidencePackages[0].source.page,4));t('property identity retained',()=>assert.equal(r.evidencePackages[0].property.propertyId,'PROPERTY-1'));t('raw evidence retained',()=>assert.equal(r.evidencePackages[0].evidence.rawLines.length,2));t('column evidence retained',()=>assert.ok(r.evidencePackages[0].evidence.columnEvidence.loading));t('token stream retained',()=>assert.ok(r.evidencePackages[0].evidence.tokenStream.length));t('parser trace created',()=>assert.equal(r.evidencePackages[0].parserTrace.length,5));t('decision types governed',()=>assert.ok(r.evidencePackages[0].allowedDecisions.includes('PARSER_BUG')));t('review is open',()=>assert.equal(r.evidencePackages[0].status,'OPEN'));t('critical count preserved',()=>assert.equal(r.summary.criticalReviews,0));t('conditional certification',()=>assert.equal(r.summary.certificationStatus,'CONDITIONALLY_CERTIFIED'));t('append-only governance',()=>assert.equal(r.governance.appendOnlyDecisionLedger,true));t('silent promotion blocked',()=>assert.equal(r.governance.silentPromotion,false));t('golden approval required',()=>assert.equal(r.governance.goldenPromotionRequiresApproval,true));t('certification run created',()=>assert.equal(r.certificationRuns.length,1));t('connector identity',()=>assert.equal(r.evidencePackages[0].connector,'AIR_CRE'));t('review field',()=>assert.equal(r.evidencePackages[0].field,'powerAmps'));t('source ambiguity suggested',()=>assert.equal(r.evidencePackages[0].suggestedClassification,'SOURCE_AMBIGUITY'));t('decision ledger starts empty',()=>assert.equal(r.decisionLedger.length,0));t('golden tests start empty',()=>assert.equal(r.goldenTests.length,0));t('review id deterministic',()=>assert.ok(r.evidencePackages[0].reviewId.startsWith('REVIEW-')));
const failures=tests.filter(x=>x.status==='FAILED');console.log(JSON.stringify({framework:'SCIIP_RELEASE_5_5_SPRINT_3_4_EVIDENCE_CERTIFICATION_PLATFORM',version:'196.4.0',status:failures.length?'FAILED':'PASSED',testsRun:tests.length,failures,result:{evidencePackages:r.evidencePackages.length,parserTraceSteps:r.evidencePackages[0].parserTrace.length,decisionTypes:r.evidencePackages[0].allowedDecisions.length,certificationStatus:r.summary.certificationStatus}},null,2));if(failures.length)process.exit(1);
