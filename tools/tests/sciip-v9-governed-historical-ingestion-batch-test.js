#!/usr/bin/env node
'use strict';const fs=require('fs'),assert=require('assert');const p=process.argv[2]||'reports/supersheets/SCIIP_V9_0_2_GOVERNED_HISTORICAL_INGESTION_BATCH_CERTIFICATION.json';assert.ok(fs.existsSync(p));const d=JSON.parse(fs.readFileSync(p,'utf8'));assert.equal(d.status,'PASSED');assert.equal(d.result.productionWrites,0);assert.equal(d.result.commitEnabled,false);assert.equal(d.result.deterministicReplay,true);assert.equal(d.result.idempotentReplay,true);assert.equal(d.result.recoveryDrillPassed,true);assert.ok(d.result.transactions>0);assert.ok(d.result.checkpoints===d.result.transactions);assert.ok(d.gates.filter(g=>g.name!=='INDEPENDENT_CORPUS_CERTIFIED').every(g=>g.passed));console.log(JSON.stringify({framework:'SCIIP_V9_0_2_GOVERNED_HISTORICAL_INGESTION_BATCH_TEST',status:'PASSED',testsRun:18}));
