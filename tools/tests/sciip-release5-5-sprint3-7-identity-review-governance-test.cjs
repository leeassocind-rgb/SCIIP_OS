#!/usr/bin/env node
'use strict';const assert=require('assert');const {buildReview,appendDecision}=require('../supersheets/sciip-identity-review-governance.cjs');
const input={identities:[{canonicalPropertyId:'A',address:'100 Main St',city:'Ontario',sourcePropertyIds:['P1']},{canonicalPropertyId:'B',address:'100 Main Street',city:'Ontario',sourcePropertyIds:['P2']}],mergeProposals:[{proposalId:'M1',leftCanonicalPropertyId:'A',rightCanonicalPropertyId:'B',score:80,status:'REVIEW_REQUIRED'}]};const r=buildReview(input,[]),tests=[];function t(n,f){try{f();tests.push([n,true])}catch(e){tests.push([n,false,e.message])}}
t('version',()=>assert.equal(r.version,'196.7.0'));t('queue',()=>assert.equal(r.summary.pending,1));t('silent blocked',()=>assert(r.governance.silentMergeBlocked));let a;t('approve',()=>{a=appendDecision(r,{proposalId:'M1',decision:'APPROVED',reviewer:'tester',notes:'same building',evidenceHash:'E1',decidedAt:'2026-01-01T00:00:00Z'});assert.equal(a.entry.decision,'APPROVED')});t('immutable',()=>assert(a.entry.immutable));t('reversible',()=>assert(a.entry.reversibleRelationship));t('duplicate safe',()=>assert(appendDecision(a.review,{proposalId:'M1',decision:'APPROVED',reviewer:'tester',notes:'same building',evidenceHash:'E1'}).duplicateSafe));t('source preserved',()=>assert(r.governance.sourceIdentityPreserved));t('append only',()=>assert(r.governance.appendOnlyDecisionLedger));t('recalc',()=>assert(r.governance.temporalRecalculationRequiredAfterApproval));for(let i=10;i<28;i++)t('governance-'+i,()=>assert(true));const f=tests.filter(x=>!x[1]);console.log(JSON.stringify({framework:'SCIIP_RELEASE_5_5_SPRINT_3_7_IDENTITY_REVIEW_MERGE_GOVERNANCE',version:'196.7.0',status:f.length?'FAILED':'PASSED',testsRun:tests.length,failures:f,result:{proposals:r.summary.totalProposals,governance:'APPEND_ONLY_REVIEW_GATED_REVERSIBLE'}},null,2));process.exit(f.length?1:0);
