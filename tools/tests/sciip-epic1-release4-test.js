'use strict';const fs=require('fs'),vm=require('vm'),path=require('path');const root=path.resolve(__dirname,'../../src/applications/industrial-data-platform');const ctx={console,JSON,Date,Math,isFinite};vm.createContext(ctx);['SCIIP_IDP_Release4_Core.gs','SCIIP_IDP_Data_Quality_Service.gs','SCIIP_IDP_Change_Summary_Service.gs'].forEach(f=>vm.runInContext(fs.readFileSync(path.join(root,f),'utf8'),ctx,{filename:f}));const jobs=[{status:'BLOCKED',warningCount:2,errorCount:1}],current=[{lifecycleState:'CURRENT',recordJson:JSON.stringify({address:'A'})}],events=[{eventId:'E',eventType:'PROPERTY_CREATED',occurredAt:'2026-07-17T00:00:00Z',actor:'test',aggregateId:'P',afterJson:JSON.stringify({address:'A'}),changesJson:'[]'}];const q=ctx.SCIIP_IDP_DATA_QUALITY_V7.calculate(jobs,current),c=ctx.SCIIP_IDP_CHANGE_SUMMARY_V7.build(events,5);const failures=[];if(q.properties!==1)failures.push('properties');if(q.missingCoordinates!==1)failures.push('coordinates');if(q.missingApns!==1)failures.push('apn');if(c.length!==1)failures.push('changes');const html=fs.readFileSync(path.resolve(__dirname,'../../src/ui/SCIIP_IDP_DataSources_Workspace.html'),'utf8');['Create Import from Active Sheet','Review queue','Commit control','What changed?'].forEach(x=>{if(!html.includes(x))failures.push('ui:'+x);});const result={framework:'SCIIP_EPIC_1_RELEASE_4_NODE_TEST',status:failures.length?'FAILED':'PASSED',testsRun:8,failures,result:{qualityScore:q.score,whatChanged:c.length,workspaceHtml:true}};console.log(JSON.stringify(result));if(failures.length)process.exit(1);
