#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path');const base=path.resolve(__dirname,'../../reports/supersheets');const p=path.join(base,'SCIIP_V8_8_CANONICALIZATION_ENGINE_CERTIFICATION.json');if(!fs.existsSync(p))throw new Error('v8.8 certification report missing');const d=JSON.parse(fs.readFileSync(p,'utf8'));const checks=[d.status==='PASSED',d.result.inputRecords>0,d.result.canonicalProperties>0,d.result.canonicalListings>0,d.result.productionWrites===0,d.result.commitEnabled===false,d.governance.stewardApprovalRequired===true,d.governance.independentCorpusCertificationRequired===true,/^[a-f0-9]{64}$/.test(d.evidenceDigest)];const failures=checks.map((x,i)=>x?null:`CHECK_${i+1}`).filter(Boolean);const out={framework:'SCIIP_V8_8_CANONICALIZATION_ENGINE_CERTIFICATION',version:'v8.8.0',status:failures.length?'FAILED':'PASSED',testsRun:checks.length,failures,result:{workspace:'supersheet-ingestion',applicationStatus:d.result.applicationStatus,canonicalProperties:d.result.canonicalProperties,canonicalListings:d.result.canonicalListings,productionWrites:0,commitEnabled:false}};console.log(JSON.stringify(out));if(failures.length)process.exit(1);
