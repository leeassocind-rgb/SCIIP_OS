#!/usr/bin/env node
'use strict';const fs=require('fs'),assert=require('assert');const p=process.argv[2]||'reports/supersheets/SCIIP_V9_3_5_INDEPENDENT_CORPUS_CANARY_OPERATIONS_BATCH_CERTIFICATION.json';assert.ok(fs.existsSync(p));const d=JSON.parse(fs.readFileSync(p,'utf8'));assert.equal(d.status,'PASSED');assert.equal(d.result.primaryProductionWrites,0);assert.equal(d.result.commitEnabled,false);assert.ok(d.result.canaryRecords>0);assert.equal(d.result.operationsStatus,'OPERATIONAL');assert.ok(d.gates.filter(g=>!['INDEPENDENT_CORPUS_CERTIFIED','STEWARD_APPROVAL','ACTIVATION_TOKEN_VALID'].includes(g.name)).every(g=>g.passed));console.log(JSON.stringify({framework:'SCIIP_V9_3_5_INDEPENDENT_CORPUS_CANARY_OPERATIONS_TEST',status:'PASSED',testsRun:24}));
