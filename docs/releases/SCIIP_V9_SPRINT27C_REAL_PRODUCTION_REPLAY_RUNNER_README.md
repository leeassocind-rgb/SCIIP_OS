# SCIIP_OS v9 Sprint 27C — Real Production Replay Runner

Sprint 27C wires the certified Sprint 27 and Sprint 27B engines to real source directories. It validates the immutable June 4 baseline, enforces June 5 as the first SuperSheet, fingerprints all source files, creates a normalization queue for any PDF date without a governed JSON observation, and only executes the historical replay when every required normalized snapshot is present.

The runner is read-only. Canonical writes remain disabled and all outputs require human review.
