# SCIIP_OS v8.0 Historical SuperSheet Ingestion Engine

Profiles the actual AIR CRE SuperSheet archive in chronological order, fingerprints each PDF, extracts candidate property addresses, compares consecutive editions, and writes non-destructive dry-run artifacts. Production commit remains disabled.

Runtime outputs: `data/supersheets/runtime-profile.json`.
