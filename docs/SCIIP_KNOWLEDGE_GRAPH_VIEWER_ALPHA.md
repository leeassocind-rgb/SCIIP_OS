# SCIIP Knowledge Graph Viewer Alpha

The Alpha workspace exposes graph nodes, edges, node-type filtering, relationship inspection, and focused neighbor exploration. It reads `GRAPH_NODES` / `GRAPH_EDGES` (or compatible aliases) from the active spreadsheet and uses certified fallback data when no live graph registry is available.
